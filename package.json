{
  "name": "mod-arch-library",
  "version": "0.0.0-semantically-released",
  "description": "Modular Architecture Essentials - A monorepo containing libraries for building scalable micro-frontend applications",
  "private": true,
  "workspaces": [
    "mod-arch-core",
    "mod-arch-kubeflow",
    "mod-arch-shared",
    "mod-arch-installer"
  ],
  "engines": {
    "node": ">=22.0.0",
    "npm": ">=10.0.0"
  },
  "scripts": {
    "build": "npm run build:core && npm run build:kubeflow && npm run build:shared && npm run build:installer",
    "build:core": "npm run build --workspace=mod-arch-core",
    "build:shared": "npm run build --workspace=mod-arch-shared",
    "build:kubeflow": "npm run build --workspace=mod-arch-kubeflow",
    "build:installer": "npm run build --workspace=mod-arch-installer",
    "test": "npm run test --workspaces --if-present",
    "test:jest": "npm run test:jest --workspaces --if-present",
    "test:core": "npm run test --workspace=mod-arch-core",
    "test:shared": "npm run test --workspace=mod-arch-shared",
    "test:kubeflow": "npm run test --workspace=mod-arch-kubeflow",
    "test:installer": "npm run test --workspace=mod-arch-installer",
    "test:installer:flavor": "npm run test:flavor --workspace=mod-arch-installer",
    "lint": "npm run test:lint --workspaces --if-present",
    "lint:fix": "npm run test:fix --workspaces --if-present",
    "lint:installer:flavor": "npm run lint:flavor --workspace=mod-arch-installer",
    "lint:installer:flavor:fix": "npm run lint:flavor:fix --workspace=mod-arch-installer",
    "clean": "rm -rf */dist */node_modules node_modules",
    "version:patch": "npm version patch --no-git-tag-version && npm version patch --workspaces --no-git-tag-version && npm run version:commit",
    "version:minor": "npm version minor --no-git-tag-version && npm version minor --workspaces --no-git-tag-version && npm run version:commit",
    "version:major": "npm version major --no-git-tag-version && npm version major --workspaces --no-git-tag-version && npm run version:commit",
    "version:commit": "git add -A && git commit -m \"chore: version bump\" --allow-empty",
    "verify-dist": "node scripts/verify-dist.mjs",
    "publish:all": "npm publish -w mod-arch-core --access public --ignore-scripts && npm publish -w mod-arch-kubeflow --access public --ignore-scripts && npm publish -w mod-arch-shared --access public --ignore-scripts && npm publish -w mod-arch-installer --access public --ignore-scripts",
    "publish:core": "npm publish -w mod-arch-core --access public --ignore-scripts",
    "publish:shared": "npm publish -w mod-arch-shared --access public --ignore-scripts",
    "publish:kubeflow": "npm publish -w mod-arch-kubeflow --access public --ignore-scripts",
    "publish:installer": "npm publish -w mod-arch-installer --access public --ignore-scripts",
    "prepare": "husky"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/opendatahub-io/mod-arch-library.git"
  },
  "homepage": "https://github.com/opendatahub-io/mod-arch-library#readme",
  "bugs": {
    "url": "https://github.com/opendatahub-io/mod-arch-library/issues"
  },
  "keywords": [
    "modular-architecture",
    "micro-frontend",
    "monorepo",
    "react",
    "typescript",
    "ui-library",
    "kubeflow"
  ],
  "author": "OpenDataHub.io",
  "license": "Apache-2.0",
  "lint-staged": {
    "mod-arch-*/**/*.{js,ts,jsx,tsx}": [
      "npx eslint --max-warnings 0"
    ]
  },
  "devDependencies": {
    "conventional-changelog-conventionalcommits": "^8.0.0",
    "@semantic-release/commit-analyzer": "13.0.1",
    "@semantic-release/exec": "7.1.0",
    "@semantic-release/github": "12.0.5",
    "@semantic-release/release-notes-generator": "14.1.0",
    "husky": "9.1.7",
    "lint-staged": "16.2.7",
    "npm-run-all2": "^7.0.1",
    "semantic-release": "25.0.3"
  }
}
